const Router = require('express');
const router = new Router();
const ProgressOfExercises = require('../controllers/ProgressExercisesController');

// router.post('/', ProgressOfExercises.create);
// router.get('/:id', ProgressOfExercises.getAll)

module.exports = router;